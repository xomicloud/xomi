"use strict";

const DEFAULT_API_HOST = "https://api.xomi.cloud",
      DEFAULT_CLIENT_URI = "/",
      DEFAULT_CLIENT_HOST = "https://authenticate.xomi.cloud";

module.exports = {
  DEFAULT_API_HOST,
  DEFAULT_CLIENT_URI,
  DEFAULT_CLIENT_HOST
};
